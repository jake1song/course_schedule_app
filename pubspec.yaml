name: course_schedule_app
description: "课表星图 - 课程表与 AI 日程助手"
publish_to: 'none'
version: 1.5.1+28

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  http: ^1.2.2
  package_info_plus: ^8.3.1
  path: ^1.9.1
  path_provider: ^2.1.5
  provider: ^6.1.2
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
